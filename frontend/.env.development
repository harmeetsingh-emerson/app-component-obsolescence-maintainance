# Local development — leave empty so App.js uses relative paths.
# The CRA dev server proxy (see package.json "proxy") forwards all
# API calls to http://localhost:8000 automatically.
REACT_APP_API_URL=
